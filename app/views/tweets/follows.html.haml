.main-contener
  .food-contener
    .food-title-contener
      .food-title-contener__title
        %p New Tweets
    %ul.tweets-list
      =render partial: "shared/tweets", collection: @FolTws, as: :tweet
    =paginate @FolTws