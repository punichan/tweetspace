.contener
  .food-contener
    .food-title-contener
      .food-title-contener__title
        %p コメントしたTweets
    %ul.tweets-list
      =render partial: "shared/tweets", collection: @comment_tweets, as: :tweet
    =paginate @comment_tweets