.contener
  .food-contener
    .food-title-contener
      .food-title-contener__title
        %p いいねしたTweets
    %ul.tweets-list
      =render partial: "shared/tweets", collection: @LikTws, as: :tweet
    =paginate @LikTws