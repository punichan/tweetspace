.contener
  %h2.sign-text New Tweet
  =form_with model: @tweet, class: "new", local: true do |f|
    .field
      =f.label :image
      %br/
      =f.file_field :image, autofocus: true, autocomplete: "image"
    .field
      =f.label :food
      %br/
      =f.text_area :food, autofocus: true, placeholder: "Up to 100 characters"
    .field
      =f.label :price
      %br/
      =f.text_area :price, autofocus: true, placeholder: "Up to 100 characters"
    -# .field
    -#   = f.collection_select :prefecture_id
    -# .field
    -#   = f.collection_select :category_id
    .field
      =f.label :tweet
      %br/
      =f.text_area :tweet, autofocus: true, placeholder: "Up to 100 characters"
    .actions
      =f.submit "send", class: "actions__btn"