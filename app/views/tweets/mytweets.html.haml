.contener
  .food-contener
    .food-title-contener
      .food-title-contener__title
        %p 自分のTweets
    %ul.tweets-list
      =render partial: "shared/tweets", collection: @mytweets, as: :tweet
    =paginate @mytweets